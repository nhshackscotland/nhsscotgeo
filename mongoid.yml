test:
  sessions:
    default:
      database: mongoid_test
      hosts:
        - localhost:27017

development:
  sessions:
    default:
      database: mongoid
      hosts:
        - localhost:27017

production:
  sessions:
      default:
        database: overridden
        hosts:
          - localhost:27017
  uri: <%= ENV['MONGOLAB_URI'] %>